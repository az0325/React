const initialState = {
    name: ""
}

export default states = (state = initialState, action) => {
    switch (action.type) {
        case "":

            break;

        default:
            break;
    }
}